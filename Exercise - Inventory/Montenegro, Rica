CREATE TABLE `exercise_1`.`supplier_item` (
  `SupplierID` INT NOT NULL,
  `Item_ID` VARCHAR(45) NOT NULL,
  `Description` VARCHAR(45) NOT NULL,
  `Price` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`SupplierID`));
